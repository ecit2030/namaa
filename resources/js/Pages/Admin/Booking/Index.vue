<template>
  <AdminLayout>
    <PageBreadcrumb :pageTitle="currentPageTitle" />
    <div class="space-y-5 sm:space-y-6">
      <ComponentCard :title="currentPageTitle">
        <ShowBookings
          :bookings="bookings"
          :consultants="consultants"
          :filters="filters"
          :statusCounts="statusCounts"
        />
      </ComponentCard>
    </div>
  </AdminLayout>
</template>

<script setup>
import PageBreadcrumb from '@/Components/common/PageBreadcrumb.vue'
import AdminLayout from '@/Components/layout/AdminLayout.vue'
import ComponentCard from '@/Components/common/ComponentCard.vue'
import ShowBookings from '@/Components/admin/booking/ShowBookings.vue'
import { usePage } from '@inertiajs/vue3'
import { computed } from 'vue'
import { useI18n } from 'vue-i18n'

const { t } = useI18n()
const currentPageTitle = computed(() => t('menu.bookings'))

const bookings = computed(() => usePage().props.bookings)
const consultants = computed(() => usePage().props.consultants)
const filters = computed(() => usePage().props.filters)
const statusCounts = computed(() => usePage().props.statusCounts)
</script>
